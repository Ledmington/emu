<?xml version="1.0"?>
<ruleset name="emu-ruleset"
		xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
		xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0 https://pmd.sourceforge.io/ruleset_2_0_0.xsd">
    <description>Emu ruleset</description>

    <rule ref="category/java/bestpractices.xml">
        <!-- Avoid reporting custom logger calls -->
        <exclude name="GuardLogStatement" />
    </rule>

    <rule ref="category/java/bestpractices.xml/AvoidReassigningLoopVariables">
        <!-- Avoid reporting increments/decrements of loop control variable outside of 'for' -->
        <properties>
            <property name="forReassign" value="skip" />
        </properties>
    </rule>

    <rule ref="category/java/codestyle.xml">
        <!-- Avoid reporting reporting variables with a short name -->
        <exclude name="ShortVariable" />
        <!-- Avoid reporting methods with early exits -->
        <exclude name="OnlyOneReturn" />
        <!-- Avoid reporting default constructors -->
        <exclude name ="UnnecessaryConstructor" />
    </rule>

    <!-- Avoid reporting missing constructors in test classes -->
    <rule ref="category/java/codestyle.xml/AtLeastOneConstructor">
        <excludePattern>.*Test.*\.java</excludePattern> <!-- Exclude all test files -->
    </rule>

    <rule ref="category/java/design.xml">
        <!-- Avoid reporting usage of classes from outside the package hierarchy -->
	    <exclude name="LoosePackageCoupling" />
    </rule>

    <rule ref="category/java/documentation.xml">
        <!-- Avoid reporting comments with too many lines -->
        <exclude name="CommentSize" />
        <!-- Avoid reporting missing comments on private fields -->
        <exclude name="CommentRequired" />
        <!-- Avoid reporting constructors with empty body -->
        <exclude name ="UncommentedEmptyConstructor" />
    </rule>

    <rule ref="category/java/errorprone.xml">
        <!-- Avoid reporting System.exit() -->
        <exclude name="DoNotTerminateVM" />
    </rule>

    <rule ref="category/java/multithreading.xml" />
    
    <rule ref="category/java/performance.xml">
        <!-- Avoid reporting redundant field initialization -->
        <exclude name="RedundantFieldInitializer" />
    </rule>

    <rule ref="category/java/security.xml" />
</ruleset>
